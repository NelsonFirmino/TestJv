export interface SubmitSearchErgon {
  idProcesso?: { label: string; value: string };
  idContador?: { label: string; value: string };
  vinculo?: string;
  matricula?: string;
  dtIni: string;
  dtFim: string;
}
