import { ChartLine } from "phosphor-react";
import styled from "styled-components";

export const Wrapper = styled.div`
  display: flex;
  flex-direction: column;
`;

export const PageIcon = styled(ChartLine)`
  font-size: 2rem;
  margin-right: 1rem;
`;
